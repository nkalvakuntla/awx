---
- name: Playbook to install NGINX
  hosts: all
  gather_facts: false
  tasks:
    - name: Ansible apt install nginx
      apt:
        name: nginx
        state: present
        update_cache: yes

